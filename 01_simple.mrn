// This is the test of two steps.

MAROON(two_steps) {
  FIBER(global) {
    FN(main) {
      STMT({
        DEBUG("hello");
        NEXT();
      });
      STMT({
        DEBUG("world");
        DONE();
      });
    };
  };
};

TEST_FIBER(
  two_steps,
  global,
  {"hello","world"});
