// This is the most trivial smoke test.

MAROON(code) {
  FIBER(global) {
    FN(main) {
      STMT({
        // This is what the test checks for.
        DEBUG("hello");
        // Essential to terminate the fiber via `DONE()`, see the next tests for invalid programs.
        DONE();
      });
    };
  };
};

TEST_SIMPLE_RUN(
  code,
  {
    // At timestamp 0 the run should debug-print "hello".
    {0, "hello"}
  })
