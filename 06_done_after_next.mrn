// TODO(dkorolev): Clean up the phrasing in this test, it's already clear in my head what it should be.

MAROON(done_after_next) {
  FIBER(global) {
    FN(main) {
      STMT({
        DEBUG("hang on tight ...");
        NEXT();
        DONE();
      });
    };
  };
};

TEST_FIBER_SHOULD_THROW(done_after_next, global, "TODO(dkorolev): FIXME: Attempted `DONE()` in the wrong place.");
